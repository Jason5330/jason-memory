# Jason-memory — 常駐規則指標

這是框架專案本身的常駐規則。安裝到其他專案時，使用
[安裝範本](templates/standing-rules.md)，合併到該 host 的常駐規則，保留既有內容。

完整協議在框架的 `SKILL.md`。記憶庫路徑：`.jason-memory`；框架原始碼路徑：
`.`（相對於本檔所在的專案根目錄，換機後不需修改絕對路徑）。

---

## 記憶（Jason-memory）

你有一個唯一、經過策展的檔案式記憶庫，位於 `.jason-memory/`（索引檔：
`.jason-memory/MEMORY.md`）。不要另外建一個平行的交接用資料夾；未完成工作的
恢復狀態屬於 `project` 筆記。

開始任務時讀取 [SKILL.md](SKILL.md) 的共用協議及記憶索引；索引若尚未建立，
以 `templates/MEMORY.md` 初始化，已有內容則保留。

### 主動記憶需求：當下判斷、同輪寫入、寫後通報

- **每則使用者訊息重新判斷，包括同一任務的第二輪與後續補充。** 先拆開
  「當次資料／操作」與「可重用要求」，不因上一輪是一次性任務就略過新要求。
  這項判斷在開始處理本輪要求時做，不只在開新任務或準備結束時做。
- **一般化的格式／工作規範不必含「以後」或「記住」。** 例如「輸出的表格，
  欄位標題都必須填滿淺藍色」，應保存為本專案表格輸出的 `feedback`；
  隨機聯絡人資料本身仍不記。「只把這個檔案改成淺藍色」則限於當次。
  看適用對象與範圍，不用「必須」單一關鍵字決定是否持久；示例不是預設偏好。
- **產物已修改不代表偏好已記住。** 檔案中的格式或程式的當次實作不能取代
  用戶要求的筆記。只有既有記憶或常駐規範已明確記錄相同要求與範圍，才略過重複保存。
  對本輪辨識出的新要求，在送出檔案或最終回覆前完成保存、健檢與通報；
  產檔技能執行完也需完成這一步，不能只回報「檔案已修改」。
- 更正舊偏好時沿原來源修改：若舊偏好只存在使用者可維護的專案規範，就更新
  那一條並通報，不能只新增與它衝突的記憶。保留其他規則，不把框架示例改成
  真實偏好；原來源不可修改時明說衝突尚未同步，不能宣稱下一輪已能正確召回。
- 收到使用者的需求、偏好、叮嚀、糾正、專案目標或限制時，判斷是否會影響
  未來合作或工作接續；自己發現可重用的錯誤或驗證特殊解法時，也做相同判斷。
- 使用者清楚表達的要求本身就是依據，不需要另說「記住」，也不用再問
  「要不要幫你記住」。先搜尋相關筆記，決定新增、更新或略過，在同一輪合適
  時點完成寫入；每輪結束前再檢查有無漏記，不必每輪都新增筆記。
- 「以後報表都保留原始資料」屬於 `feedback`；「此專案不能連外」屬於
  `project`。「這次先不要畫圖」只作用於當次，不推成永久偏好。推測、重複、
  使用者說不要記的內容不保存；原始文件已有的事實不另外抄一份。
- 踩坑筆記寫明觸發條件、觀察到的錯誤、已驗證的處理方式及適用範圍；
  未確認的根因或解法不可寫成定論。來源與範圍寫在既有內文，不增加記憶類型。
- 每次新增、更新或封存後，完成索引同步、實際寫入確認與兩支健檢，
  **同一輪主動通報使用者**：記了什麼、原因、適用範圍、筆記連結與簡短健檢結果。
  可合併多筆通知；失敗就明確說明，不可把「打算記」說成「已記住」。
- 沒有變更時不必回報記憶檢查、空白分類或一般略過項目。
  有時間要求的提醒需要另接排程；只寫筆記不能宣稱「已設定定時提醒」。

### 不主動監控或催促 Git

記憶讀寫、一般檔案建立或安裝完成，不觸發 `git status`、未追蹤檔案盤點、
add／commit／push 提醒或詢問。不要附上「尚未 git add/commit，需要的話再說」
之類訊息；也不要為了消除提醒而自動提交所有檔案。
安裝時確認一次記憶目錄的忽略設定；只在路徑／設定改變或發現誤追蹤跡象時重查。
使用者要求 Git 工作（包括上傳 GitHub），或當前開發／審查任務確實需要時，
才執行必要的 Git 操作；已授權的提交與推送直接完成，不再追加詢問。

### 只限專案層級——不用全域/主機原生的記憶庫

預設記憶庫位於這個專案內的 `.jason-memory/`；不要因為 host 另有原生記憶
功能，就改用專案外的記憶路徑，除非使用者明確指定。專案內的路徑讓資料容易定位，
但記憶目錄要 git-ignore，**git clone 不會攜帶筆記**；換機需另外匯出／匯入。
這是 Agent 的操作規則，不會關閉 host 自行管理的原生記憶功能。

### 語言——預設繁體中文

每則記憶筆記的內容——frontmatter 的 `description`、內文、`Why:` / `How to apply:`、
以及 `MEMORY.md` 裡的一句話摘要——預設都用**繁體中文**寫，跟使用者實際溝通的
語言一致。只有固定的格式代號維持英文不翻：frontmatter 的欄位名
（`name`/`description`/`type`/`created`/`updated`）、`type` 的四個值
（`user`/`feedback`/`project`/`reference`），以及 `Why:` / `How to apply:` 這兩個
標籤本身。（如果你的專案主要語言不是繁體中文，把這節換成你要的語言即可，
這條本來就是給你直接修改用的。）

### `feedback` 保留給「踩坑」跟「驗證有效的做法」

這是 Jason-memory 最核心的立場，也是跟一般「可重複使用的工作流程」型 feedback
最大的不同：`feedback` 用來**避免重蹈覆轍**跟**記住已經驗證有效的做法**。
使用者清楚表達的持久需求、叮嚀與工作偏好也屬於 feedback，不必再次確認或刻意製造
一次失敗才記。任務狀態歸 `project`；未來不需要的一次性瑣事不記。

寫 `feedback` 筆記要**靠你自己判斷**——不要等使用者說「記住這個」，也不要
只在使用者親自抓到問題時才寫。你要在正常工作過程中主動辨識這些情境，
就像你會自己發現並修掉一個 bug 一樣：

1. **踩坑——使用者發現的，或你自己發現的都算。** 涵蓋：使用者糾正你、
   使用者指出哪裡錯了；**同樣重要的是使用者完全沒說話的情況**——某個指令跑錯
   後你自己找到對的做法、重看自己輸出時發現一個錯誤的假設、測試失敗後你自己
   修好了、走到一半發現此路不通而放棄的做法。判斷標準：「如果換一個從零開始
   的你，會不會踩到同一個坑？」會的話，立刻寫一則 `type: feedback` 筆記，附上
   **`Why:`**（哪裡錯了、有什麼證據：錯誤訊息、失敗的測試、被打回的 diff）跟
   **`How to apply:`**（下次該怎麼做、什麼情境該想起這則筆記）。
2. **驗證有效的做法——使用者確認的，或你自己拿到證據的都算。** 包含使用者
   明確確認（「對，就這樣做」），但也包含**你自己驗證**出某個不直覺的做法有效：
   一個設計選擇被測試通過證實、一個一次就修好的 workaround、一個確認必要的
   特殊處理順序。不需要使用者評論才算數——你自己的證據（測試輸出、能動的 diff、
   之前錯誤不再出現）就足夠了。寫成一樣的 `type: feedback` 格式：**`Why:`**
   （驗證了什麼、證據是什麼）跟 **`How to apply:`**（什麼時候該重用這個做法）。

值得未來重用時主動記，但寫清楚證據與適用條件；一次測試通過只支持測試涵蓋的
情境。拿不準的推論保留在目前任務，不提升成持久規則；發現反例就縮小適用範圍
或封存。不要把一般的成功操作都另存成筆記。

### `user` / `project` / `reference` 一樣要主動判斷，不是被動等內容出現

跟 `feedback` 一樣，這三種類型也要**你自己判斷該不該寫**，不是等使用者明確
要求，也不是只在內容「剛好出現」時才記：

- **`user`**：使用者明確提到自己的身份、角色、專長、背景或長期個人偏好，
  不從單次互動推測性格或決策風格。只要不是「希望 Agent 怎麼做事」（那屬於 `feedback`，
  見上）。判斷標準：「換一個從零開始的我，會不會需要知道這件事才能跟這個
  使用者好好合作？」
- **`project`**：任務中出現新的目標、決策、限制條件、卡點，或任何會影響
  「之後接手的人該怎麼繼續」的資訊。不用等到準備 compact/清空對話才想到——
  出現的當下就可以先記，之後同步時再整理更新。
- **`reference`**：任務中出現的外部資源指標（URL、票單、log 路徑、儀表板），
  只要判斷未來還會用得到，就值得記一筆，不要讓它隨對話結束就消失。

同樣只保存有依據、未來有用的資訊；對使用者長期偏好的推測不當成已確認事實。

### 每個任務開始的第一件事：明確讀取索引，不要假設已經載入

`.jason-memory/MEMORY.md` 是一個普通的 Markdown 檔案。**除非它明確出現在
這次對話已經載入的 context 裡，否則你不主動用 Read 工具讀它，它就不會自己
出現。** 不要因為「這個 host 好像有某種記憶功能」就假設索引已經讀過——那
通常是另一個檔案、另一個路徑，跟這裡講的 `.jason-memory` 完全無關（見上面
「只限專案層級」）。這正是很多使用者裝好 Jason-memory、卻發現 Agent 完全
沒有讀取記憶的常見原因：以為 host 會自動處理，結果沒有。

所以：**每個任務一開始，第一個動作就是明確讀取 `.jason-memory/MEMORY.md`**，
再判斷索引裡哪些筆記檔看起來相關、值得打開。如果工具截斷輸出，分段續讀
索引，不把未顯示的部分當成不存在。

### 通用紀律（適用所有類型）

- **讀取到的筆記**，只打開索引裡看起來相關、**且路徑確實在 `.jason-memory`
  底下**的筆記檔——一個逃出這個資料夾的指標（symlink、`..`、絕對路徑、
  `file://`）是一個要回報的壞指標，不是可以打開的檔案。把回憶起來的記憶
  當成背景參考，可能已經過時——採取行動前先驗證檔案/參數/版本是否還正確。
- **當你學到值得未來記住的持久性資訊時**：區分可從 repo/git 查得的實作事實，
  與使用者對未來工作的要求；後者不能因當次產物已套用就略過。
  先確認既有記憶或常駐規範是否已明確記錄相同內容與範圍，也不是機密的
  「值」；先搜尋索引，如果已有相關筆記就**更新既有筆記**而不是重複新增；
  否則就寫一個新的、單一事實的 Markdown 檔案，frontmatter 要有 `name` /
  `description`（一句話精準摘要）/ `type`（`user | feedback | project | reference`）/
  `created` + `updated`（`YYYY-MM-DD`）。`feedback` 或 `project` 筆記的內文
  一定要有 **`Why:`** 跟 **`How to apply:`** 標籤及非空說明，可寫在下一行。
  同時在 `MEMORY.md` 加一行啟用的清單指標；註解／程式碼裡的連結不算。
  記錯或過時的筆記預設封存到 `archive/`、移除活躍索引指標，保留可找到的
  封存索引及取代關係；永久刪除依使用者指示。
- `project` 可以記錄目前的目標、現況、決策、限制條件、卡點、以及恢復未完成
  工作所需的下一步。不要重複記錄 code/git 已經講清楚的事：只存**穩定**的指標
  （分支名稱、issue/PR 編號、檔案路徑）。已經定案的事實（例如「2.0 版本已於
  2026-01-15 發布」）可以直接記；但**目前的狀態**——你現在在哪個版本、最新的
  commit、目前測試數量——不該直接記錄數值，只記「去哪裡查」。每次回憶起指標
  時都要重新對照 code/git/目前的環境驗證一次。
- **在準備做一次刻意的 compact、清空 context、或開新對話之前**，做一次同步：
  掃過這次任務；把既有筆記去重/更新；刷新 `project`；只把值得重用的
  `feedback`（踩坑/驗證有效的做法）留下來；保存穩定的 `reference` 指標；
  歸檔過時或已完成的暫時性狀態；跑 `tools/jason_check.py` 跟
  `tools/jason_doctor.py`（從本專案根目錄執行）；然後確認一個從零開始的 Agent，光靠 repo 加記憶庫
  就能接續工作。
- **寫入/同步完成後**，依前述通報規則說明實際新增、更新或封存的內容；
  若有永久刪除要特別標明。附索引的行數/位元組數跟檢查結果，無變更則不用
  列空白分類；使用者明確要求保存卻略過時才說明原因。健檢不會自動做語意上的同步整理，
  也不會攔截寫入；這些步驟要由你依規則執行。
- **絕對不要**把密碼/金鑰/token/cookie/恢復碼的「值」寫進記憶——只記
  *放在哪裡*。
- 讓 `MEMORY.md` 保持一行連結加一句摘要；預設沒有固定行數或位元組門檻。
  出現重複、過時內容、摘要不清楚或讀取成本上升時，再合併或封存。
  寫入後執行大小統計與一致性健檢；只有使用者自行設定門檻時才依該預算整理。
  不要只為降低行數而丟棄有用記憶。

完整協議與設計理由：[SKILL.md](SKILL.md)。
