# 解壓後，直接開始

1. 將 ZIP 解壓到自己的工作資料夾，例如桌面上的 `我的AI工作區`。
2. 在支援本機檔案及專案規則的 AI 工具中，開啟解壓後包含 `AGENTS.md` 的
   `jason-memory` 資料夾，建立新對話。Codex 用本機資料夾，不選 Git worktree。
3. Claude Code 建議先執行一次 `INSTALL.cmd`，合併 hooks 並固定目前 Python 路徑；
   不清空、不重建已有記憶。重新開對話並確認 `/hooks` 已載入。
4. 直接說明你的工作，例如：「幫我整理這些資料；本專案的回報都先列結論。」
   AI 應按規則判斷、保存可重用需求並通報，後續工作讀回記憶。

不需要安裝 Git、執行 clone、建立分支、提交、推送或建立 PR。
需要能讀寫本機檔案並載入規則的 AI 工具；健檢使用 Python 3.9+，可用工具
內建的 Python。不需要額外 Python 套件。

一般使用版已附空白 `.jason-memory/MEMORY.md`。若下載的是原始碼 ZIP，
第一次使用由 AI 從範本建立索引，仍不需要 Git。主動記憶後應收到包含內容、
適用範圍及筆記連結的通報；沒有通報時，可要求 AI 檢查是否已載入本資料夾
的 `AGENTS.md` 和 `SKILL.md`，不能只憑檔案存在就假定有生效。

Claude Code 使用根目錄 `CLAUDE.md` 的匯入行，自動載入同一份索引。
第一則回覆前仍須讀取適用筆記；正常召回不輸出記憶流程說明。
若自行將入口搬到 `.claude/CLAUDE.md`，匯入路徑要改成 `@../.jason-memory/MEMORY.md`。

## 若看到 master 或 Create PR

這是 AI 工具的版本控制介面。請確認開啟的是新解壓的一般資料夾，而不是
原本 clone 的儲存庫，也不要把使用資料夾放進其他儲存庫裡。檔案 `.gitignore`
只是排除清單，不會將資料夾變成儲存庫；`.git` 資料夾或檔案才是相關中繼資料。

框架規則只能約束 AI 的操作與回覆，不能強制隱藏宿主 App 的按鈕。
不需要為了關掉介面而刪除原始開發專案的歷史。

## 個人記憶與更新

記憶保存在工作資料夾的 `.jason-memory/`。它是明文檔案，分享給別人前須
移除個人筆記；不要將自己整個使用資料夾重新壓縮當作公開版。

更新時先備份自己的 `.jason-memory/`，將新版解壓到另外的資料夾，再將自己的
記憶庫完整複製過去，取代新版附的空白記憶庫。不要反過來用新版空白索引
覆蓋舊記憶；文件與其他工作產物按需要另行搬移。開新對話後讓 AI 執行健檢。

升級後在新版資料夾重新執行 `INSTALL.cmd`，再開 Claude 新對話確認 `/hooks`。
本版會核對保存宣告與本輪工具收據、實際筆記；沒有寫入卻說「已記住」時，
會要求補寫或更正。重試仍無法驗證則顯示失敗警告，不能當作保存成功。
只看到 `.hook-state`、`.receipts` 或 `.writer.lock` 不代表偏好已記錄，須有實際筆記。

詳細說明見 [README.md](README.md)。

跨筆記更正、保存通知與 hook 限制見 [runtime 協議](docs/memory-runtime.md)。
