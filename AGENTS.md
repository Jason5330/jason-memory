# Jason-memory — Codex 專案規則

## 統一讀寫與通知（優先於下方手動流程）

先讀 [runtime 協議](docs/memory-runtime.md)。每輪用
`python tools/memory_runtime.py --config .jason-memory.json context` 取得同一記憶庫
最新內容；Claude hook 已提供完整且未變更的正文時可重用，截斷時補讀。
所有變更經 runtime 的 `apply --plan`，不直接 Write/Edit 筆記或索引。
更正時搜尋所有相關筆記，批次更新同一事實，保留其他事實；新／更正事實加
穩定的 `jason-facts` 識別欄位。每次新增、修改、封存成功後，在最終回覆末尾
顯示「已將此要求記憶在……」，附實際絕對路徑的 Markdown 連結、範圍及健檢結果。
失敗明說未保存；單純召回安靜執行，不輸出「我來讀記憶」等流程語言。
宣告保存成功前，確認本輪 runtime 的 apply／verify 回傳驗證收據，
保留完整工具 JSON 輸出，並連結實際筆記；不要只說已記住。既有偏好不需重寫，
可執行 `verify --scope project --note 相對筆記路徑` 讀回驗證；失敗時撤回成功宣告。


## 回覆前先召回

每則使用者訊息，在第一則可見文字（含進度說明）前，先讀取目前的
`.jason-memory/MEMORY.md`，再讀與回覆行為、開場、語言、格式及任務相關的
有效筆記。簡單問答也適用；先召回再回覆，不先宣告「我來讀記憶」。
只有本輪已載入完整且最新索引時可省略重讀；後續訊息重新讀取，避免沿用更正前內容。
摘要不取代相關正文；遵守適用範圍及目前使用者要求、較高優先規則。
正常召回保持安靜，不說「已讀取記憶」「已套用偏好」「本次無需記憶」。
只在實際新增、修改或封存後通報；讀寫失敗才說明問題，不宣稱已召回。

本框架可直接在下載解壓後的一般資料夾使用，不依賴 Git 或 GitHub。
不要為啟用記憶而執行 Git 指令、建立儲存庫、連接遠端或要求使用者提交。
索引不存在時從範本建立即可；不要覆寫既有記憶。只有安裝目標已知使用 Git
時才處理忽略設定，普通資料夾略過；不要為此每輪探測儲存庫。

## 每則使用者訊息都適用，包括第二輪以後

處理本輪要求前，先分開辨識「當次資料／操作」與「可重用的用戶要求」。
上一輪判斷不需記憶，只對上一輪的資訊有效，不能沿用為整串對話的結論。
「輸出的表格，欄位標題都必須填滿淺藍色」是本專案表格輸出的 `feedback`
要求，即使是在修改一次性測試檔時提出，也要主動保存；不需要出現「以後」
或「記住」。相對地，「只把這個檔案改成淺藍色」只限當次；不能僅靠「必須」
一詞判定持久性。示例用來辨識語意，不是預先套用的顏色偏好。

若辨識出新的可重用要求，先搜尋既有記憶，在送出檔案或最終回覆前完成
筆記／索引更新、健檢與同輪通報；沒有完成時要明說未保存，不能只交付檔案。
當次檔案已套用格式，不代表用戶要求已存在於可召回的記憶或常駐規則。
一般任務的產檔／傳送流程不取代這項要求。沒有變更則保持安靜。

## 路徑與共用協議

本專案的記憶庫為 `.jason-memory/`，框架路徑為本專案根目錄。
開始任務時讀取 [CLAUDE.md](CLAUDE.md) 與 [SKILL.md](SKILL.md) 的共用記憶規則，
明確讀取 `.jason-memory/MEMORY.md` 及相關筆記；索引尚未建立時使用
`templates/MEMORY.md` 初始化，保留既有筆記。

使用者清楚表達的持久需求、偏好、叮嚀、糾正、專案限制，以及 AI 自行驗證的
可重用踩坑經驗，都要主動判斷並在適當時點保存；不等「記住」指令、不再詢問
是否保存。每輪結束前檢查漏記；一次性要求不推成長期偏好。
寫入後同步索引、跑兩支健檢，並於同輪通報內容、原因、範圍與筆記連結；
未成功不得宣稱已記住。無變更不必回報空白記憶狀態。

記憶工作不觸發 Git 狀態監控或 add／commit／push 提醒，不附「尚未提交，需要再說」
之類訊息。僅在使用者要求 Git 工作或當前任務確實需要時執行必要操作。
此規則不代表每次檔案變更都自動提交。

安裝到其他專案請使用 [安裝範本](templates/standing-rules.md)，設定實際記憶與框架路徑。
