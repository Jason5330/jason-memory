# Jason-memory：跨電腦、跨 AI 開發交接

這是接手開發的入口。即使沒有前一段聊天紀錄，也可以從這裡取得目標、程式版本、歷史證據、已知限制與下一步。以下檔案都是專案交接資料，不是宿主的全局指令，也不會自行啟用記憶。

## 給下一個 AI 的任務背景

使用者要的是：AI 自主辨識可重用的需求、偏好、糾正與已驗證教訓；真正保存後通知使用者；新對話能召回，後續更正不衝突。同一台電腦上的不同 AI 可共用資料，專案需求與全局偏好要隔離。普通記憶不觸發 Git 監控、提交提醒或冗長流程說明。

目前採 Markdown 筆記加索引。已新增統一 runtime、事實識別、版本化寫入、Claude hooks、保存證據及快速保存流程。不要在沒有測量與重現前，因為「模型能力差」或「架構太重」就重寫整個系統。

使用者曾要求在原開發電腦真正解除安裝，已執行且核對。**那是本機安裝與工作目錄的清除，不代表 GitHub 原始碼應被刪除，也不代表已要求在新電腦安裝全局版。** 換機接手請取得新工作副本；不要因讀到舊入口規則就自動恢復原電腦的記憶庫。

## 先看哪些文件

1. 先讀本檔，了解目前狀態與待驗證項目。
2. 讀 [40 項踩坑經驗](docs/踩坑經驗總結.md)，優先看開頭十項原則，再看此次任務相關章節。
3. 需要佐證時才看 [歷史測試報告](docs/experience-evidence/tests/hooks-validation.md) 與 [來源清單](docs/experience-evidence/sources.json)，不用每輪重讀全部證據。
4. 真正改程式時，讀當前 [runtime 協議](docs/memory-runtime.md) 及相關原始碼。`docs/experience-evidence/` 是歷史快照，不取代當前程式。

## 程式與資料的位置

| 路徑 | 責任 |
|---|---|
| `tools/memory_runtime.py` | canonical 設定、context、remember、apply、verify、交易與收據 |
| `tools/memory_facts.py` | 結構化事實與部分舊筆記矛盾辨識 |
| `global/global_store.py` | 全局／專案隔離、鎖、底層檔案操作 |
| `tools/claude_memory_hook.py` | 啟動／每輪注入、路徑護欄、保存證據與通知檢查 |
| `tools/hook_settings.py` | 合併／移除自己擁有的 Claude hooks |
| `tools/install_hooks.py`、`global/install.py` | 專案／全局安裝；保留其他設定 |
| `tools/jason_check.py`、`tools/jason_doctor.py` | 索引及筆記結構健檢，不能取代完整語意驗證 |
| `tools/build_download.py`、`tools/build_global_download.py` | 以明確清單建立兩種乾淨 ZIP |
| `UNINSTALL.bat`、`tools/purge_memory.py` | 明確範圍的永久清除及預覽；不要拿真實資料做測試 |
| `tests/test_memory_runtime.py`、`tests/test_purge_memory.py` 等 | 自動化回歸測試 |
| `tests/behavioral-memory.md`、`evals/evals.json` | 真實 Agent 多輪行為驗收情境 |

專案記憶由 `.jason-memory.json` 指定，預設 `.jason-memory/`。全局版預設使用目前使用者家目錄下 `.jason-memory-global/`，shared 與各專案分開。路徑不能寫死為任何前任開發者的 Windows 帳號。

## 已完成的基線

交接前的程式基線是提交 [`a1db633e7ffbc870b1cd02b638bbc09899ad2150`](https://github.com/Jason5330/jason-memory/commit/a1db633e7ffbc870b1cd02b638bbc09899ad2150)。本次交接文件是後續文件變更；不要把舊基線當成永遠最新版本。

- 有入口匯入與 hooks 的新對話召回、兩輪間外部更新測試。
- 有同一事實跨筆記更正、保留其他事實、並行競爭及中斷重播測試。
- 成功宣告要有本輪工具收據；曾重現完全沒寫入卻說已保存的漏檢並修補。
- 簡單偏好可用一次 `remember`，同值免重寫；複雜更正仍走版本化批次。
- 普通 PostToolUse 不再掃整庫，必要的寫入與 Stop 檢查保留。
- 提供專案／全局 ZIP 與清除工具；清除不能刪掉自訂安裝目錄內的其他工作檔。
- 該程式提交的 [CI](https://github.com/Jason5330/jason-memory/actions/runs/35409347191) 在 Ubuntu／Windows、Python 3.9／3.13 四組環境通過。本機清除相關 19 個案例通過。歷史略過測試與行為限制見證據文件。

本機解除安裝當時核對 103 個檔案已移除、2 個設定保留其他內容、工作檔不變。仍保留清除工具、Git 歷史與 17 個舊 ZIP；不宣稱電腦上不存在任何副本。公開版 [清除核對紀錄](docs/experience-evidence/cleanup-verification.redacted.json) 已移除原電腦帳號路徑。

後續整理本交接文件時，原電腦的全局目錄與 Codex 全局入口已再次存在，本專案記憶目錄仍不存在。重建來源未查明；本次文件整理未執行安裝，也未再次刪除。接手時應核對所在電腦的即時狀態，不用歷史報告推論現在必然已安裝或已卸載。

## 不得誤認為已解決的問題

| 下一步 | 已知證據／缺口 | 建議驗收 |
|---|---|---|
| P1：不同模型與宿主的真實端到端驗證 | 未直接測 MiniMax 服務；Claude 有限樣本不代表所有模型必定成功 | 使用者提供可用環境後，按同一測試腳本記錄 host、模型、權限、工具輸出、磁碟與通知；沒有授權或權限就明確標未驗證 |
| P1：跨 AI 雙向寫入 | 一次 Codex 可讀回 Claude 所寫偏好，但更正被唯讀／執行政策擋住；不能當作雙向寫入通過 | 在已獲准可寫的隔離專案做 A 寫、B 讀／改、A 再讀，以及同時寫入；不繞過權限 |
| P1：語意漏記與舊筆記矛盾 | 結構化同鍵檢查已具備；自由文字、別名、正文與 metadata 語意仍非完全可判定 | 加入含稱呼＋職業、否定句、條件偏好、多人物、混合正文的正反例；不得只比對格式就宣稱理解正確 |
| P1：保存延遲與上下文成本 | 對照單樣本由 78.385 秒、9 次工具降至 34.752 秒、1 次工具；仍不是秒級保證 | 同條件多次冷／暖測試，分開記模型往返、程序啟動、runtime、hooks 與含快取 usage；確認護欄沒退化 |
| P2：筆記庫增大 | 100 筆合成更新的局部中位數仍約 6.545 秒；Stop 仍有整庫檢查 | 以更多筆記與真實工作負載定位瓶頸，再評估索引或增量驗證；不先取消必要檢查 |
| P2：新對話、compact、跨日與缺 hook | 早期日期測試未跨日；完整快照與截斷的分支不同 | 跨日、壓縮、無 hook、PARTIAL、外部更正後重新開對話，均測首句與最新偏好 |
| P2：部署與卸載相容性 | 安裝路徑、信任條件、舊版管理標記及自訂來源可能不同 | 測不同帳號、中文／空白路徑、既有 host 規則、原生 auto-memory 開／關、升級與殘留報告 |

這是下一步工作清單，不代表未驗證項目都是已確認程式 bug。先重現、定位，再修補；不能把權限阻擋或模型行為問題一律改成更重的框架規則。

## 換電腦後的安全起步

完整開發原始碼與交接文件在 [GitHub 專案](https://github.com/Jason5330/jason-memory)。可 clone 或使用 GitHub 的 **Code → Download ZIP**。舊 release 的一般使用 ZIP 不一定包含本次交接文件，也不等於完整開發目錄。

```text
git clone https://github.com/Jason5330/jason-memory.git
cd jason-memory
```

使用 ZIP 則直接進入解壓後的原始碼目錄，不需為執行記憶而安裝 Git。記下當前版本；Python 3.9+ 是原基線的工具需求。

開發測試不需要先安裝全局版。先看程式與測試，再於新工作副本執行：

```text
python -X utf8 -m unittest discover -s tests -v
```

Windows 若要讓測試的子程序也統一 UTF-8，可在 PowerShell 先設：

```powershell
$env:PYTHONUTF8 = '1'
python -m unittest discover -s tests -v
```

需要驗證全局安裝時，只對新建的暫存目錄使用 `global/install.py --user-home <隔離帳號目錄> --home <隔離記憶目錄>`；先確認目標是測試資料。**不要直接執行無參數的全局 INSTALL 或 UNINSTALL 作為煙霧測試。** 沒有建立 symlink 的權限就記錄略過，改由適合的平台測，不關閉保護來湊通過數。

真正的模型行為測試還需要所選宿主、可用模型與本機工具權限。本交接包不含登入憑證、私人筆記或可直接沿用的帳號設定。若新 AI 只能聊天、不能存取檔案或執行命令，它可以分析文件，但不能聲稱已實作、寫入或驗證程式。

## 可直接貼給下一個 AI 的交接提示

```text
請接手 Jason-memory 的開發，不假設你看過先前聊天。

先讀專案根目錄 HANDOFF.md，再讀 docs/踩坑經驗總結.md。
需要證據時讀 docs/experience-evidence/；修改前以目前原始碼為準。

目標：主動辨識可重用需求、真實保存後通知、跨對話召回、更正一致，
並減少不必要工具往返。保留 Markdown 架構，先重現問題再決定修法。
不要每輪重讀整套文件，不要加入 Git 日常監控或記憶讀取碎念。

先確認當前版本、環境和工具權限，在隔離資料中跑既有測試。
依 HANDOFF.md 的未完成項目選擇與我本次問題相關的測試，
把程式錯誤、入口／路徑問題、權限問題、模型行為分開定位。
保留寫入後驗證、同一事實跨筆記更正、版本衝突與資料保護。

未經我要求，不要安裝到真實全局目錄、恢復已刪記憶或跑真實清除。
不要把測試通過推論為所有模型都可靠；沒有實際執行就說未驗證。
完成後交代改動、證據、效能影響與剩餘限制，並更新交接文件。
```

## 如何維護這份交接

每次實質修補後更新「已完成基線」與未完成表，附實際提交／測試紀錄；保留歷史證據原文及雜湊，不把新推測回填成舊測試結果。若發現舊結論不準確，明確記更正原因。

公開交接只保存工程知識、合成案例與去識別化證據。個人偏好、帳號憑證、真實筆記與完整私人對話，不能為了方便換機就一起打包進 GitHub。
